% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dpost.R
\name{n1post}
\alias{n1post}
\alias{nun1post}
\alias{dn1post}
\alias{pn1post}
\alias{qn1post}
\alias{rn1post}
\title{Posterior Distribution for n=1 Data}
\usage{
nun1post(fam = c("normal", "cauchy"), ddist = NULL, ...)

dn1post(
  x,
  A,
  obs,
  nu = NULL,
  fam = c("normal", "cauchy"),
  ddist = NULL,
  log = FALSE,
  ...
)

pn1post(
  q,
  A,
  obs,
  nu = NULL,
  fam = c("normal", "cauchy"),
  pdist = NULL,
  lower.tail = TRUE,
  log.p = FALSE,
  ...
)

qn1post(
  p,
  A,
  obs,
  nu = NULL,
  fam = c("normal", "cauchy"),
  qdist = NULL,
  pdist = NULL,
  ...
)

rn1post(n, A, obs, nu = NULL, fam = c("normal", "cauchy"), rdist = NULL, ...)
}
\arguments{
\item{fam}{One of \code{"normal"} or \code{"cauchy"}. If
\code{ddist}, \code{pdist}, \code{qdist}, or \code{rdist} are specified
then this argument is ignored.}

\item{ddist}{Density function of standard distribution \eqn{\rho()}. Should have a \code{log} argument.}

\item{...}{Additional arguments for \code{ddist}, \code{pdist},
\code{qdist}, and \code{rdist}.}

\item{x, q}{vector of quantiles}

\item{A}{The prior value.}

\item{obs}{The observed value. This is \eqn{X} in the description.}

\item{nu}{The prior value of nu. If not known, use \code{nun1post()}}

\item{log, log.p}{logical; if \code{TRUE}, probabilities p are given as log(p).}

\item{pdist}{Cumulative distribution function of standard distribution.}

\item{lower.tail}{logical; if \code{TRUE} (default), probabilities are P(X<=x)
otherwise, P(X>x).}

\item{p}{vector of probabilities}

\item{qdist}{Quantile function of standard distribution.}

\item{n}{sample size}

\item{rdist}{Random generation of standard distribution.}
}
\value{
density, distribution, quantile, or random values.
}
\description{
Density, probability, quantile, and random generation from the posterior
distribution of the location parameter when one has \eqn{n = 1} observation
and uses a particular prior that results in valid confidence intervals
(asymptotically in the confidence level).
}
\details{
Let \eqn{X} have PDF \eqn{\frac{1}{\sigma}\rho((x - \mu)/\sigma)} for a
known and symmetric \eqn{\rho(\cdot)}. Let \eqn{\nu = |\mu - A| / \sigma}
for a pre-specified \eqn{A}. We place prior \eqn{\pi(\mu) = |\mu - A|^{-1}}
and \eqn{\nu = \text{argmax}_{a > 0}a\rho(a)} with probability 1.

These functions will calculate the posterior distribution and allow you to
interact with it through the distribution, density, quantile, and random
generation functions.
}
\section{Functions}{
\itemize{
\item \code{nun1post()}: Obtains prior value of \eqn{\nu}.

\item \code{dn1post()}: Density function.

\item \code{pn1post()}: Distribution function.

\item \code{qn1post()}: Quantile function.

\item \code{rn1post()}: Random generation.

}}
\examples{
set.seed(1)
# Observe x = 2, assume t with 2 df, prior value is A = 1
nun1post(ddist = dt, df = 2) ## nu should be 1
x <- seq(-10, 10, length.out = 500)
y <- dn1post(x = x, A = 1, obs = 2, nu = 1, ddist = dt, df = 2)
z <- rn1post(n = 10000, A = 1, obs = 2, nu = 1, rdist = rt, df = 2)
z <- z[z >= -10 & z <= 10]
graphics::hist(z, freq = FALSE, breaks = 200, border = "grey", col = "grey")
graphics::lines(x, y, type = "l", col = "#E69F00")
graphics::abline(v = c(1, 2), col = c("#56B4E9", "#009E73"), lty = c(2, 3))
pn1post(q = 2, A = 1, obs = 2, nu = 1, pdist = pt, df = 2)
qn1post(p = 0.7113, A = 1, obs = 2, nu = 1, qdist = qt, pdist = pt, df = 2)

}
\author{
David Gerard
}
